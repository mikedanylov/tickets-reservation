<div class="container container-centre">
  <% if logged_in? && current_user.is_admin %>
    <!-- load javascript for changing seats status -->
    <%= javascript_include_tag 'update_status.js' %>
  <% end %>
  <%= render partial: "seats_table" %>
</div>
